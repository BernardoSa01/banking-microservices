server:
  port: 8083
  address: 0.0.0.0

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: banking
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
